# HubSpot Email Style-Learning Log

> Auto-appended by `hubspot-send-stats-tracker` once a send is confirmed 5+ days old. Compares the original markdown draft (what Claude wrote) against the final shipped content (what actually went out, via HubSpot's PREVIEW_CONTENT) and describes what changed in plain terms — subject, preview text, structure, tone, CTA wording, cuts.
>
> This is a raw log, not a set of rules. Individual entries are not standing guidance — a pattern only becomes a rule once `hubspot-style-pattern-review` (monthly) spots it repeating across 2+ sends and Jackie confirms it. See `context/brand-voice.md` for confirmed voice rules and the `feedback_*` memory files for confirmed per-channel edit patterns (LinkedIn, client re-engagement).

---

_No entries yet — the first will land once a HubSpot-drafted send clears the 5-day stats window._
